apiVersion: security.istio.io/v1beta1
kind: AuthorizationPolicy
metadata:
  name: allow-ingress-gateway
  namespace: apps
spec:
  selector:
    matchLabels:
      app: httpbin
  action: ALLOW
  rules:
  - {} # <--- Regla vacía significa: "Coincide con TODO"
